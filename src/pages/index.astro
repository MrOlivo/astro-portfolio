---
import { information } from "@data";

import Layout from "@layouts/Layout.astro";
import Hero from "@components/home/Hero.astro";
import Experience from "@components/home/Experience.astro";
import ProjectsWrapper from "@components/general/ProjectsWrapper.astro";
import MetaHead from "@components/general/MetaHead.astro";

const featureProjects = information.projects.reduce((accumulator, project) => {
  if (accumulator.length < 6 && project.isFeatured) {
    accumulator.push(project);
  }
  return accumulator;
}, []);
---

<!doctype html>
<html lang="es" class="scroll-smooth">
  <head>
    <MetaHead
      title={"Portafolio"}
      description={information.about}
      ogImageUrl={"/assets/images/profile.png"}
    />
  </head>

  <body>
    <Layout>
      <Hero name={information.name} about={information.about} />
      <Experience
        title={"Experiencia"}
        details={information.experience}
        id="experience"
      />
      <Experience
        title={"Educación"}
        details={information.education}
        id="education"
      />
      <section id="projects" class="bg-light dark:bg-dark">
        <h2 class="w-[15rem] pt-16 dark:text-light uppercase">Proyectos</h2>
        <ProjectsWrapper projects={featureProjects} />
      </section>
    </Layout>
  </body>
</html>
